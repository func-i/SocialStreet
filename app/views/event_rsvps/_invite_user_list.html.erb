<ul id="invite_user_list">
  <%(user_list || []).each do |connection|%>
    <%= render :partial => 'event_rsvps/invite_user_element.html', :locals => {:user => connection.to_user} %>
  <% end %>
</ul>
