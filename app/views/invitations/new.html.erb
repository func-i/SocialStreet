<%= render 'invitations/invitation_tool' %>

<% content_for :end_of_body do %>
  <%= javascript_include_tag 'ss_pageless' -%>
  <script>
    $(function() {
/*)    $('#empty_modal_sidebar').find('.content').pageless({
        container: '#empty_modal_sidebar .content',
        totalPages: <%= @num_pages_invitations -%>,
        url: '<%= new_event_rsvp_invitation_path(@event, @rsvp) %>',
        loaderMsg: 'Loading more results',
        params: $('form.search-params-invitations').serializeObject(),
        currentPage: <%= params[:page] || 1 %>
      });*/
    });
  </script>
<%- end -%>