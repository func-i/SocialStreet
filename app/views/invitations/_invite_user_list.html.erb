<% if current_user.try(:facebook_friends_imports) %>
  <%(@invitation_user_connections || []).each do |connection|%>
    <%= render :partial => 'invitations/invite_user_element.html', :locals => {:user => connection.to_user} %>
  <% end %>
<% else %>
  <%=image_tag "loading.gif", :id=>'loading_invitation_image'%>
<% end %>
