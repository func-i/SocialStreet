<% broadcast "/chat_rooms/#{@message.chat_room.id}" do %>
  var $chatContent = $("#chat_<%= @message.chat_room_id %> .chat-content");
  $chatContent.append("<%= escape_javascript render(@message) %>");
  $chatContent.scrollTop($chatContent.attr('scrollHeight'));
<% end %>
setupTipsy();