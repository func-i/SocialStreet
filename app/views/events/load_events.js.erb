clearMarkers();
<%- @searchables.each do |searchable| -%>    
  <% if searchable.geo_located? %>
    placeEventMarker(new google.maps.LatLng(<%= searchable.location.latitude %>, <%= searchable.location.longitude %>), '#');
  <%- end -%>
<%- end -%>