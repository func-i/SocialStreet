<div class="profile">
  <div class="profile-holder">
    <%= image_tag(url_for_event_image(@event), :size => "80x80", :class => "avatar") %>
    <div class="text">
      <div class="row">
        <div class="text-content">
          <h2><%= @event.name %></h2>
          <p>
            <% if l = @event.location %>
              <% if l.street? %>
                <%= l.street %><br/>
              <% end %>
              <% if l.city? || l.state? %>
                <%= l.city %> <%= l.state %><br/>
              <% end %>
              <% if l.country? %>
                <%= l.country %><br/>
              <% end %>
            <% end %>
          </p>
        </div>
        <a href="#" class="btn-share">Share</a>
      </div>
    </div>
    <div class="info">
      <div class="col">
        <a href="#" class="btn-attend"><span>Attend</span></a>
        <% if @event.editable_time? %>
          <% if @rsvp%>
            <%- unless @event.canceled? -%>
              <p>or <%= link_to "Invite People", [:change, @event, @rsvp, :invitations], :class => "button" %></p>
            <%- end -%>
          <%- end -%>
        <%- end -%>        
      </div>
      <div class="col-holder">
        <div class="col">
          <span>Start Day:</span>
          <strong><%= @event.starts_at.strftime("%B %d") %></strong>
        </div>
        <% if @event.finishes_at %>
          <div class="col">
            <span>End Day:</span>
            <strong><%= @event.finishes_at.strftime("%B %d") %></strong>
          </div>
        <%- end -%>
        <div class="col">
          <span>Start Time:</span>
          <strong><%= @event.starts_at.strftime("%l:%M %p") -%></strong>
        </div>
        <div class="col">
          <span>End Time:</span>
          <strong><%= @event.finishes_at.strftime("%l:%M %p") -%></strong>
        </div>
      </div>
    </div>    
  </div>
  <div class="keyword">
    <%- @event.searchable_event_types.each do |set| -%>
      <%= link_to set.name, '#', :onclick => "return false;" -%>
    <%- end -%>
  </div>
</div>
<div class="post-message">
  <%= form_for [@event, @comment], :remote => true, :html => {"data-type" => "html",:class => "form-post-message Post-Message form comment-form" } do |form| %>
    <fieldset>
      <div class="text">
        <%= form.text_field :body, :title => "Post a Message" -%>
      </div>
      <div class="info">
        <p>Your message will be posted to <span>X (address)</span> with the keywords <span>Y (keyword)</span>.</p>
        <p>Others in this community will be able to see and reply to your post.</p>
      </div>
    </fieldset>
  <%- end -%>
  <%= render 'shared/commenting' %>
</div>
<div id="comment-box" class="post-holder Post-Holder" >
  <div id="action-list">
    <% @actions.each do |action| %>
      <%= render 'shared/action', :action => action %>
    <% end %>
  </div>
</div>

<% content_for :sidebar do %> 

  <div class="box map">
    <div class="heading">
      <h3>
        <% if l = @event.location %>
          <% if l.text %>
            <strong><%= l.text %></strong><br/>
          <% end %>
          <% if l.street? %>
            <%= l.street %><br/>
          <% end %>
          <% if l.city? || l.state? %>
            <%= l.city %> <%= l.state %><br/>
          <% end %>
          <% if l.country? %>
            <%= l.country %><br/>
          <% end %>
        <% end %>
      </h3>
    </div>
    <div class="map-holder">
      <% if @event.geo_located? %>
        <img src="http://maps.google.com/maps/api/staticmap?zoom=14&size=317x217&maptype=roadmap&sensor=false&markers=color:red%7Clabel:A%7C<%= @event.latitude %>,<%= @event.longitude %>"
             alt="location map" />
           <% end %>
    </div>
  </div>

  <div class="box people-list">
    <div class="heading">
      <h3>Attending</h3>
      <a href="#" class="btn-view">View all</a>
    </div>
    <div class="box-content">
      <ul class="list-img">
        <% @event.attendees_rsvps_list.each do |attendee_rsvp| %>
          <li><%= render 'shared/user_list_element', :user => attendee_rsvp.user%></li>
        <% end %>
      </ul>
    </div>
  </div>

  <div class="box people-list">
    <div class="heading">
      <h3>Organizers</h3>
      <a href="#" class="btn-view">View all</a>
    </div>
    <div class="box-content">
      <ul class="list-img">
        <% if @event.user == current_user %>
          <%= link_to "Add", [:new, @event, :administrator], :class => "button" %>
        <% end %>
        <% @event.administrators_rsvps_list.each do |administrator_rsvp| %>
          <li><%= render 'shared/user_list_element', :user => administrator_rsvp.user%></li>
        <% end %>
      </ul>
    </div>
  </div>

  <div class="box people-list">
    <div class="heading">
      <h3>
        <%if @event.maximum_attendees%>
          Waiting (<%= @event.num_waiting%>)
        <%else%>
          Maybe (<%= @event.num_maybe_attending%>)
        <%end%>
      </h3>
    </div>
  </div>

  <div class="block notice">
    <p>TODO Format for these actions!</p>
    <%= link_to_popup_modal "FooBar", "some-div" -%>
    <h4>Actions</h4>
    <% if @event.editable_time? %>
      <% if @rsvp%>

        <% if !@event.canceled? %>
          <%= link_to "Edit RSVP (#{@rsvp.status})", [:edit, @event, @rsvp], :class => "button" %>
          <%= link_to "Invite People", [:change, @event, @rsvp, :invitations], :class => "button" %>
        <% end %>

        <% if @event.editable_by?(current_user) %>
          <%= link_to "Edit Event", [:edit, @event], :class => "button" %>
        <% end %>

        <%- unless @rsvp.posted_to_facebook? -%>
          <%= link_to "Post to Facebook", [:post_to_facebook, @event], :class => "button" -%>
        <%- end -%>

      <% elsif !@event.canceled? %>
        <%= link_to 'Join Event', [:new, @event, :rsvp], :class => "button" %>
        <%= link_to "Post to Facebook", [:post_to_facebook, @event], :class => "button" -%>
      <% end %>

    <% end %>

    <div class="clear"></div>
  </div>

<% end %>

<div id="some-div" class="pop-up1" style="display:none">
  <div class="heading">
    <strong>Add a new activity</strong>
    <%= link_to "close", '#', :class => "btn-close" -%>
  </div>
  <div class="pop-up1-content">
    <form action="#">
      <fieldset>
        <div class="row">
          <label for="what">What?</label>
          <input id="what" type="text" title="What?" value="Add keywords" class="text" />
        </div>
        <div class="row">
          <label for="where">Where?</label>
          <input id="where" type="text" title="Where?" value="Enter Address" class="text" />
        </div>
        <div class="row-map">
          <img width="497" height="229" src="images/img-map.png" alt="image description"/>
        </div>
        <div class="row-when">
          <span>When?</span>
          <div class="col">
            <span><label for="date">Start date:</label></span>
            <input id="date" title="Start date" type="text" value="June 11th" class="date" />
          </div>
          <div class="col">
            <span><label for="time">Start Time:</label></span>
            <input id="time" type="text" title="Start Time" value="8:00" class="time" />
            <select class="sel" title="Start Time">
              <option>PM</option>
              <option>AM</option>
            </select>
          </div>
          <a href="#">+ add an End Date and Time</a>
        </div>
        <div class="title-row">
          <p><a href="#">+ Title, Description, Event Icon</a></p>
        </div>
        <div class="row-btn">
          <a href="#" class="btn">Create</a>
						or <a href="#">cancel</a>
        </div>
      </fieldset>
    </form>
  </div>
</div>

