<%= form_for @event_for_create, :remote => true, :html => {:class => "modal-submit-form form", :multipart => true, :onsubmit => "return checkButtonPressed();"} do |form| %>
  <%= render 'events/form', :form => form, :event => @event_for_create %>
<% end %>