<% if searchable.event %>
  <%= render 'event', :event => searchable.event %>
<% elsif searchable.comment %>
  <%= render 'shared/action', :action => searchable.comment.action %>
<% else  %>
  <% unless Rails.env.production? %>
    <p>YOU SHOULD NOT SEE THIS (DEBUG MESSAGE)</p>
  <% end %>
<% end %>